# DA Manager Release Checklist — v0.1.0-rc.1

## Release metadata

| Field | Value |
|---|---|
| Release version | `0.1.0-rc.1` |
| Release date | 2026-06-06 |
| Release owner | Ron Yacketta |
| Security reviewer | Ron Yacketta |
| Change type | Normal |
| Risk rating | Medium |
| Target environment | Development / Test self-hosted server management |
| Release branch/tag | `v0.1.0-rc.1` ready to tag |
| Build artifact | Local `./update.sh` artifact validated |

## Change summary

```text
Summary:
First controlled pre-1.0 release candidate for DA Manager.

Scope:
- secure localhost-default admin foundation
- Discord auth/session/link groundwork
- read-only Player 360 foundation
- inventory request/order coordination
- Farming Requests UI
- Inventory Studio action-history and stack-size groundwork
- AppSec endpoint audit foundation
- generated route auth-boundary coverage
- high-risk/destructive mutation audit-event coverage
- blocked-mutation audit coverage
- route-specific audit target metadata coverage
- changelog/ledger compaction

Out of scope:
- Discord-driven full server management
- Live Admin RMQ command execution
- Welcome Kits
- raw command publishing UI
- Player 360 mutation/self-service expansion
- formal SOC 2 or ISO certification readiness
```

## Risk and impact assessment

| Question | Response |
|---|---|
| Does this affect authentication or authorization? | Yes. Discord/session groundwork and strict admin-token behavior are part of the release history. |
| Does this affect SSH, tunneling, or database connectivity? | Yes. Existing hardening and update-script validation affect operator setup and runtime safety. |
| Does this affect player mutation workflows? | Yes. Audit and mutation-safety controls affect high-risk mutation handling. No new mutation behavior is introduced by release setup. |
| Does this affect audit logging? | Yes. Multiple audit coverage slices are included. |
| Does this affect frontend token handling? | Yes. Browser Access Key/session-storage hardening is part of the validated history. |
| Does this affect deployment/network exposure? | Yes. Backend remains localhost-bound by default with fail-closed non-loopback behavior. |
| Does this introduce new dependencies? | No new runtime dependency is introduced by release setup. |
| Does this require secret rotation? | No required rotation for release setup; rotate if deploying to a new host or if secrets may have been exposed. |

## Required validation gates

### Build and test

| Gate | Command | Result | Evidence location |
|---|---|---|---|
| Canonical update | `./update.sh` | Pass | Local terminal output, reported clean build 2026-06-06 |
| Go tests | included in `./update.sh` | Pass | Local terminal output |
| Go build | included in `./update.sh` | Pass | Local terminal output |
| Frontend install | included in `./update.sh` | Pass | Local terminal output |
| Frontend typecheck | included in `./update.sh` | Pass | Local terminal output |
| Frontend lint | included in `./update.sh` | Pass | Local terminal output |
| Frontend build | included in `./update.sh` | Pass | Local terminal output |

### Security validation

| Gate | Command | Result | Evidence location |
|---|---|---|---|
| npm audit | included in `./update.sh` | Pass | Local terminal output |
| Go vulnerability scan | `govulncheck ./...` | Deferred for RC | Future evidence before final `0.1.0` if feasible |
| Go static security scan | `gosec ./...` | Deferred for RC | Future evidence before final `0.1.0` if feasible |
| Secret scan | `gitleaks detect --source .` | Deferred for RC | Future evidence before final `0.1.0` if feasible |
| Filesystem/container scan | `trivy fs --severity HIGH,CRITICAL .` | Deferred for RC | Future evidence before final `0.1.0` if feasible |
| Dependency review | GitHub dependency review or equivalent | Deferred for RC | Future evidence before final `0.1.0` if feasible |
| SBOM generation | `syft . -o spdx-json` or equivalent | Deferred for RC | Future evidence before final `0.1.0` if feasible |

## Manual security checks

| Check | Expected result | Result |
|---|---|---|
| Browser Network tab contains no `ws_token` | Pass | Previously validated by AppSec slices; spot-check still recommended before final `0.1.0` |
| WebSocket URL contains only one-time stream ticket | Pass | Previously validated by AppSec slices; spot-check still recommended before final `0.1.0` |
| Reusing a consumed WebSocket ticket fails | Pass | Previously validated by AppSec slices; spot-check still recommended before final `0.1.0` |
| Expired WebSocket ticket fails | Pass | Previously validated by AppSec slices; spot-check still recommended before final `0.1.0` |
| `LISTEN_ADDR=0.0.0.0:8080` without override fails startup | Pass | Previously validated by AppSec slices; spot-check still recommended before final `0.1.0` |
| Non-loopback bind with documented override starts only when intentionally configured | Pass | Previously validated by AppSec slices; spot-check still recommended before final `0.1.0` |
| Unknown or changed SSH host key is rejected | Pass | Deferred spot-check before final `0.1.0` |
| Non-Ed25519 SSH client key is rejected | Pass | Deferred spot-check before final `0.1.0` |
| Weak or malformed `ADMIN_TOKEN` is rejected | Pass | Covered by backend tests and prior AppSec validation |
| Frontend rejects weak or malformed Browser Access Key | Pass | Covered by frontend validation and prior AppSec validation |
| High/destructive mutation without reason is rejected when enforcement is enabled | Pass | Covered by blocked-mutation audit coverage validation |

## Documentation updates

| Document | Required update | Status |
|---|---|---|
| `VERSION` | Set to `0.1.0-rc.1`. | Done |
| `docs/release-versioning.md` | Add release numbering policy. | Done |
| `CHANGELOG.md` | Add compact release setup entry. | Done |
| `PATCH_NOTES.md` | Add current release setup note. | Done |
| `docs/RELEASE_CHECKLIST.md` | Existing reusable release checklist template. | Done |
| `docs/changelog/unreleased/` | Keep detailed per-slice records. | Done |

## Rollback plan

```text
Rollback trigger:
- release candidate validation fails in a way that cannot be fixed safely before tagging
- new critical/high security issue is identified
- update script cannot produce repeatable build artifacts

Rollback steps:
1. Do not create or publish the tag if tagging has not occurred.
2. If tagged, delete local and remote v0.1.0-rc.1 tag only if it has not been consumed externally.
3. Revert release setup commits if necessary.
4. Return VERSION to the prior value or dev state if release is abandoned.
5. Record the failed candidate in PATCH_NOTES.md and the release checklist.

Expected rollback duration:
15–30 minutes for docs/config-only rollback.

Data compatibility concerns:
None for release setup. No schema or player-data mutation was added.

Operator communications:
Notify operators that v0.1.0-rc.1 is deferred or superseded and continue using the previous validated commit.
```

## Secret rotation checklist

| Secret | Rotate? | Evidence |
|---|---|---|
| `ADMIN_TOKEN` | No, unless deploying to a new host or exposure is suspected. | Accepted for RC |
| DA Manager SSH key | No, unless deploying to a new host or exposure is suspected. | Accepted for RC |
| DB password | No, unless deploying to a new host or exposure is suspected. | Accepted for RC |
| GitHub tokens/secrets | No, unless exposure is suspected. | Accepted for RC |
| Reverse proxy credentials/certificates | No, unless public deployment is configured. | Accepted for RC |

## Known issues and risk acceptance

| Issue | Severity | Risk decision | Owner | Review date |
|---|---|---|---|---|
| Update-script modularization is started and needs continued validation. | Medium | Accept for `rc.1`; mitigate before final `0.1.0` if instability appears. | Ron Yacketta | Before `0.1.0` |
| SAST/DAST/govulncheck/gitleaks/trivy/SBOM evidence is not yet recorded. | Medium | Accept for `rc.1`; required before final `0.1.0` if feasible. | Ron Yacketta | Before `0.1.0` |
| Discord-driven full server management is not implemented or security-reviewed. | High | Out of scope for `0.1.0-rc.1`. | Ron Yacketta | Future `0.2.0+` planning |
| Browser token storage remains JavaScript-readable during active session. | Medium | Accept as known pre-1.0 limitation; future memory-only or secure session-cookie auth target. | Ron Yacketta | Before `1.0.0` |

## Approval

| Role | Name | Date | Approval |
|---|---|---|---|
| Release owner | Ron Yacketta | 2026-06-06 | Approved for RC tag |
| Security reviewer | Ron Yacketta | 2026-06-06 | Approved for RC tag with documented deferrals |
| Operations owner | Ron Yacketta | 2026-06-06 | Approved for RC tag |

## Post-release verification

| Check | Result | Notes |
|---|---|---|
| Backend starts cleanly | Pending | Complete after tag/artifact install or launch. |
| Frontend loads cleanly | Pending | Complete after tag/artifact install or launch. |
| Connectivity diagnostics pass | Pending | Complete against intended test server. |
| Audit events are written | Pending | Complete after tag/artifact install or launch. |
| Logs stream using one-time tickets | Pending | Complete after tag/artifact install or launch. |
| No new high/critical findings | Pending | Complete when external scans are run. |

## Release decision

```text
Approved to tag v0.1.0-rc.1

Decision rationale:
The canonical local update/build path passed clean. Remaining security scan and manual verification gaps are documented as release-candidate deferrals, not final-release acceptance. Live Admin, RMQ, Welcome Kits, raw command publishing, and Discord-driven full server management remain explicitly out of scope.

Follow-up actions:
1. Create and push annotated tag v0.1.0-rc.1 from the validated commit.
2. Create a GitHub release for v0.1.0-rc.1.
3. Attach or link local build artifacts if desired.
4. Perform post-release verification checks.
5. Record any fixes as v0.1.0-rc.2 or promote to v0.1.0 after acceptance.
```
