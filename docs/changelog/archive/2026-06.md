# June 2026 Changelog Archive

## Purpose

This file is an archive index for June 2026 release and security-audit activity.

The prior full inline changelog became too large for safe repeated connector-based replacement. The complete pre-compaction history is preserved in Git history instead of being copied into another oversized Markdown file.

## Full pre-compaction changelog source

The last full inline changelog before the ledger split is available in commit:

```text
05c6cc17d133a0815af0fb1be0fc5cc1e8d53d40
```

File:

```text
CHANGELOG.md
```

## June 2026 work areas already represented in Git history

- AppSec endpoint audit initialization
- Discord auth/session route validation
- Discord player link and self-service foundation
- Farming Requests / inventory request-order coordination
- Discord bot command adapter skeleton
- Inventory Studio action history
- Inventory Studio stack-size edit planning and implementation
- AppSec findings `ASEA-001` through `ASEA-006`
- generated full-route auth-boundary coverage
- high-risk/destructive mutation audit-event coverage
- database endpoint hardening
- infrastructure/log endpoint hardening
- browser-token/CORS hardening

## Archive policy

Do not paste large historical changelog content into this file. Link to immutable commits and create small per-slice records under:

```text
docs/changelog/unreleased/
```

When a release is cut, move or summarize validated unreleased records into a compact monthly archive file.
